name: sync-submissions

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger sync
        run: |
          curl -fsS -X GET "${{ secrets.SCOREBOARD_URL }}/api/sync?secret=${{ secrets.CRON_SECRET }}"
